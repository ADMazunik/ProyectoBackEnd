<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse align-self-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/realtimeproducts">Add/Delete Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/chat">Chat</a>
                </li>
            </ul>
        </div>
        {{#if loggedIn}}
        <div class="d-flex align-items-center ">
            <p class="text-white fs-5" style="max-height: 20px;">Bienvenido
                {{#if isAdmin}}
                <strong style="color: gold;">ADMIN {{username}}</strong>
                {{else}} <strong>{{username}}</strong>
                {{/if}}
            </p>
            <button onclick="location.href='/logout'" class="btn btn-sm btn-danger mx-4">LogOut</button>
        </div>
        {{/if}}
    </div>
</nav>